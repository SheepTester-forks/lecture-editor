{
  "scripts": {
    "build": "esbuild --bundle src/index.tsx --loader:.jpg=file --outdir=public --supported:nesting=false --sourcemap --minify",
    "dev": "esbuild --bundle src/index.tsx --loader:.jpg=file --outdir=public --servedir=public --sourcemap"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react-dom": "^18.2.25",
    "esbuild": "^0.20.2"
  }
}
